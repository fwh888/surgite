# Base URL for the standup-gen API.
# Leave unset to use the built-in defaults: http://localhost:8000 during `npm run dev`,
# and same-origin (empty) in a production build served by FastAPI.
# Set this only to point the frontend at a non-default backend.
# VITE_API_BASE=http://localhost:8000
