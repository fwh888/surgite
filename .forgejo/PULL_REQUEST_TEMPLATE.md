<!-- Thanks for contributing. Keep PRs focused — one logical change is easiest to review. -->

## What this changes

A short description of the change and why.

## Related issue

Closes #…  <!-- or remove if N/A -->

## Checklist

- [ ] Ran `uv run ruff check .` and `uv run ruff format .`
- [ ] Ran `uv run mypy backend/` and `uv run pytest`
- [ ] Ran `npm run check` and `npm run test` in `frontend/` (if the frontend changed)
- [ ] Added/updated tests for the change
- [ ] Updated docs (`README.md` / `AGENTS.md`) and `CHANGELOG.md` `[Unreleased]` if behavior changed
